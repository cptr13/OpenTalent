# --- OpenTalent essentials ---

## Secrets (untracked local config & scans)
config/database.php
config/*.local.php
config/*.ini
.env
.env.*
.secrets-audit/
debug_mysql_command.txt
*.secret.json
service-account*.json

## Uploads & runtime
# Root uploads: ignore contents, keep folder via .gitkeep
uploads/**
!uploads/.gitkeep

# Temp restore dir: keep folder, ignore contents
tmp_restore/**
!tmp_restore/.gitkeep

# Fully ignore public/uploads (should not exist in repo)
public/uploads/

## Dependencies (optional to commit)
vendor/
node_modules/

## Logs / dumps / noise
*.log
*.bak
*.tmp
*.swp
.DS_Store
.idea/
.vscode/

# SQL dumps (but keep canonical schema)
*.sql
!config/schema.sql

## Debug / runtime artifacts
INSTALL_LOCKED
debug_reset_*.txt
public/debug*.txt
